{
  "name": "farmacia-cr",
  "version": "1.0.0",
  "description": "Sistema de gestión Farmacia C&R",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "open": "node -e \"const {exec}=require('child_process');setTimeout(()=>{const cmd=process.platform==='win32'?'start http://localhost:3000':process.platform==='darwin'?'open http://localhost:3000':'xdg-open http://localhost:3000';exec(cmd);},1500);\" & nodemon index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
